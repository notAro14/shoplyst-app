import { css } from "src/stitches.config"

export const heading = css({
  lineHeight: 1.3,
})()
export const loaderContainer = css({
  width: "100%",
  height: 300,
  display: "grid",
  placeItems: "center",
})()
